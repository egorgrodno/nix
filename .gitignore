*.md
!README.md
!CLAUDE.md

# nixos-rebuild build / nix build output symlinks
result
result-*

# Per-machine Claude Code overrides; the shared settings are .claude/settings.json
.claude/settings.local.json
